# 作業系統期末考複習講義
## Ch5（排程演算法）– Ch9（虛擬記憶體）｜ 2011–2016 歷屆考題分析

---

## ⚡ 超高頻必考主題（幾乎每年都考！）

1. **多層反饋佇列甘特圖 + 平均等待時間**（Ch5）
2. **銀行家演算法**（判斷請求可否批准 / 證明 unsafe）（Ch7）
3. **頁表大小計算 + 多層分頁填內容**（Ch8）
4. **頁面置換計算**（Optimal vs LRU 的 page fault 數）（Ch9）
5. **同步程式填空**（Peterson / TestAndSet / Readers-Writers / Producer-Consumer）（Ch6）

---

# Chapter 5 — CPU Scheduling（從排程演算法開始）

## 5.1 排程準則（Scheduling Criteria）

| 準則 | 定義 | 目標 |
|------|------|------|
| CPU utilization | CPU 使用率（40%~90%）| **最大化** |
| Throughput | 單位時間完成的行程數 | **最大化** |
| Turnaround time | 行程完成的總時間（等待+執行+I/O）| 最小化 |
| Waiting time | 在 ready queue 等待的總時間 | 最小化 |
| Response time | 從提交到第一個回應的時間 | 最小化（互動系統重視）|

> **Waiting time = Turnaround − CPU burst − I/O time**（排程只影響等待時間）

---

## 5.2 ★ 排程演算法比較

| 演算法 | 描述 | 搶占性 | 優點 | 缺點 |
|--------|------|--------|------|------|
| **FCFS** | 先來先服務，FIFO queue | 非搶占 | 簡單 | **Convoy effect**（護航效應），平均等待時間長 |
| **SJF** | 選下一個 CPU burst 最短的 | 可搶占/非搶占 | **平均等待時間最短（optimal）** | 無法預知 burst 長度 |
| **Priority** | 優先權最高先（低數字=高優先）| 可搶占/非搶占 | 靈活 | **Starvation（飢餓）** |
| **Round-Robin** | 每個 time quantum 輪流 | 搶占 | 公平、response time 好 | quantum 太大→FCFS；太小→context switch 開銷大 |

### FCFS — Convoy Effect（護航效應）
一個 CPU-bound 大行程霸佔 CPU，許多 I/O-bound 小行程在 ready queue 排隊乾等 → CPU 與 I/O 裝置使用率都下降。

### SJF 為何 optimal？
把短行程排在長行程前面，**短行程減少的等待時間 > 長行程增加的等待時間**，所以平均等待最小。
- 預測下次 burst：**指數平均** τₙ₊₁ = α·tₙ + (1−α)·τₙ（α=0.5 時新舊各半）
- 搶占式 SJF = **Shortest-Remaining-Time-First（SRTF）**

### Priority — Starvation 與 Aging
- **Starvation（飢餓）**：低優先權行程可能永遠不執行。
- **Aging（老化）**：隨等待時間逐漸提高優先權（如每 15 分鐘 +1），最終必被執行。
- SJF 是 priority 的特例（burst 越大優先權越低）。

### Round-Robin 重點
- ready queue 當作**環狀佇列**，新行程加到尾端。
- n 個行程、quantum=q，每個最多等 **(n−1)×q**。
- quantum 極大 → 退化成 **FCFS**；quantum 極小 → **processor sharing**（但 context switch 開銷大）。
- 經驗法則：**80% 的 CPU burst 應比 quantum 短**。

---

## 5.3 ★★ 多層佇列 vs 多層反饋佇列

| | Multilevel **Queue** | Multilevel **Feedback** Queue |
|--|----------------------|-------------------------------|
| 行程能否換佇列 | **不能**（固定）| **能**（依行為升降層）|
| 規則 | 各佇列獨立演算法，佇列間固定優先權 | CPU 用太多→降層；等太久→升層 |
| 通用性 | 普通 | **最通用**（但最複雜）|

### ★★ 多層反饋佇列考試標準設定
```
Q2（最高）：Round-Robin，quantum = 4ms  ← 新行程進入此層
   ↓ 用完 quantum 或被搶占 → 降層
Q1（中）  ：Round-Robin，quantum = 6ms
   ↓ 用完 quantum 或被搶占 → 降層
Q0（最低）：FCFS
```
**關鍵規則：**
1. 新行程 / I/O 完成回來 → 進入 **Q2**。
2. 高層行程**搶占**低層行程。
3. 用完 quantum 或被搶占 → **降一層**。
4. 解題口訣：畫時間軸，每次選**最高層**的行程，注意 I/O 完成回 Q2 會搶占。
5. 等待時間 = 完成時間 − 到達時間 − CPU 總長 − I/O 時間。

> 詳細甘特圖解答見 [期末考古題解答.md](期末考古題解答.md) 題型 1。

---

## 5.4 多處理器排程（Multiple-Processor Scheduling）

| 概念 | 重點 |
|------|------|
| **Asymmetric** | 一個 master 處理所有排程決策，其餘為 slave |
| **Symmetric (SMP)** | 每個處理器自我排程；現代主流 |
| **Processor affinity** | 行程傾向留在同一處理器（保留 cache）。Soft / Hard affinity |
| **Load balancing** | **Push migration**（kernel 推）+ **Pull migration**（閒置處理器拉）|

> 注意：load balancing 與 processor affinity **互相抵觸**（搬移會使 cache 失效）。

---

## 5.5 執行緒排程（Thread Scheduling）

- **PCS（Process-Contention Scope）**：同一行程內的 user threads 競爭 LWP。
- **SCS（System-Contention Scope）**：kernel threads 競爭 CPU。
- One-to-one 模型（Windows XP）只用 **SCS**。

---

## 5.6 OS 排程範例

| OS | 排程方式 |
|----|----------|
| **Solaris** | 優先權式、6 類別、time-sharing 用 multilevel feedback queue（優先權與 time slice 成反比）|
| **Windows** | 優先權式、搶占、32-level，foreground 加大 quantum |
| **Linux CFS** | Completely Fair Scheduler，用 **vruntime**（虛擬執行時間），選 vruntime 最小的；nice 值 −20~+19 |

---

## 5.7 演算法評估（Algorithm Evaluation）

1. **Deterministic modeling（確定性建模）**：給定 workload 算出確切數字，簡單快速但只適用該案例。
2. **Queueing models（排隊模型）**：用 **Little's formula** `n = λ × W`（n=佇列長、λ=到達率、W=等待時間）。
3. **Simulations（模擬）**：用亂數產生器或 trace tape，較準但昂貴。
4. **Implementation（實作）**：最準，但成本最高。

---

# Chapter 6 — Synchronization

## 6.1 ★ 競爭條件與臨界區

- **Race condition（競爭條件）**：多個行程同時存取共享資料，結果取決於存取**順序**。
  - 例：`counter++` 與 `counter--` 並行 → counter 可能是 4、5 或 6。
- **Critical Section（臨界區）**：存取共享資料的程式碼段。

### ★ 臨界區解法的三個需求
1. **Mutual Exclusion（互斥）**：同時只能有一個行程在臨界區。
2. **Progress（進展）**：沒人在臨界區時，想進入的行程中要能在有限時間決定誰進入。
3. **Bounded Waiting（有限等待）**：一個行程提出請求後，其他行程進入臨界區的次數有上限。

### 臨界區結構
```c
do {
    entry section      // 請求進入
    critical section
    exit section       // 離開
    remainder section
} while (TRUE);
```

> **Nonpreemptive kernel** 天生無 race condition；**Preemptive kernel** 須小心設計（但反應快、適合 real-time）。

---

## 6.2 ★★ Peterson's Solution（兩行程軟體解）

```c
// 共享：int turn; boolean flag[2];
do {
    flag[i] = TRUE;                    // 我想進入
    turn = j;                          // 但先讓你
    while (flag[j] && turn == j);      // 你想進且輪到你 → 等

    // CRITICAL SECTION

    flag[i] = FALSE;                   // 我離開

    // REMAINDER SECTION
} while (TRUE);
```
滿足三需求（互斥靠 turn 只能一值；progress、bounded waiting 見解答）。

---

## 6.3 ★★ 同步硬體（TestAndSet / Swap）

```c
boolean TestAndSet(boolean *target) {   // 原子操作
    boolean rv = *target;
    *target = TRUE;
    return rv;
}
// 簡單互斥（不滿足 bounded waiting）
do {
    while (TestAndSet(&lock)) ;  // busy wait
    // critical section
    lock = FALSE;
} while (TRUE);
```
- **Atomic（原子）= 不可中斷**。
- 簡單版滿足互斥但**不滿足 bounded waiting** → 需用 `waiting[n]` 陣列版（見解答題型 7）。

---

## 6.4 ★ 號誌（Semaphores）

```c
wait(S) {                signal(S) {
    while (S <= 0) ;         S++;
    S--;                  }
}
```
- **Binary semaphore（= mutex lock）**：值 0/1，初值 1，解互斥。
- **Counting semaphore**：值無限制，初值 = 資源數量，控制有限資源。
- **wait()/signal() 必須原子！**（否則互斥被破壞，見題型 12）

### 無忙等實作（block/wakeup）
- wait() 中 S<0 時把行程 `block()` 放入等待佇列（waiting state），不忙等。
- signal() 中 `wakeup()` 喚醒一個等待行程（→ ready）。
- |S 負值| = 等待中的行程數。

### 同步應用（強制執行順序）
P1 的 S1 要在 P2 的 S2 之前：synch 初值 0；P1：`S1; signal(synch)`，P2：`wait(synch); S2`。

### 號誌的問題
- **Deadlock**：兩行程互等對方的號誌。
- **Starvation**：LIFO 順序可能讓某行程永遠等待。
- **Busy waiting（spinlock）**：浪費 CPU，但多處理器短時間鎖有用（免 context switch）。

---

## 6.5 ★★ 經典同步問題

### Bounded-Buffer（有界緩衝 / 生產者-消費者）
```c
// semaphore mutex=1, empty=n, full=0
// Producer
wait(empty); wait(mutex);
  // 放入 buffer
signal(mutex); signal(full);

// Consumer
wait(full); wait(mutex);
  // 取出 buffer
signal(mutex); signal(empty);
```
> 口訣：**先 wait 資源號誌（empty/full），再 wait(mutex)**；順序顛倒會死結！

### Readers-Writers（讀者-寫者）
```c
// semaphore mutex=1, wrt=1; int readcount=0
// Reader
wait(mutex); readcount++;
  if (readcount==1) wait(wrt);   // 第一個 reader 鎖 writer
signal(mutex);
  // reading
wait(mutex); readcount--;
  if (readcount==0) signal(wrt); // 最後一個 reader 放 writer
signal(mutex);

// Writer
wait(wrt);  /* writing */  signal(wrt);
```
第一類問題：**reader 優先**（除非 writer 已取得權限，否則 reader 不等）。

### Dining-Philosophers（哲學家用餐）
- 5 位哲學家、5 支筷子，每人需左右兩支才能吃。
- **直接用 semaphore chopstick[5] 會死結**（全部同時拿左筷）。
- 解法：①最多 4 人同時上桌 ②兩支都可用才拿 ③奇偶數先拿不同邊（asymmetric）。
- **無死結 ≠ 無飢餓**。

---

## 6.6 Monitors（監視器）

- 高階語言建構，把共享變數+程序包成模組，**同一時間只有一個行程能在 monitor 內活躍**。
- **Condition variable**：`x.wait()`（阻塞）、`x.signal()`（喚醒一個等待者；無人等待則無效）。
  - 對比：semaphore 的 signal **一定**改變值；monitor 的 signal 無人等待時**無效**。
- **兩種 signal 模型**：
  - **Signal and wait**：喚醒者先跑，發訊號者等。
  - **Signal and continue**：發訊號者繼續，被喚醒者等。
- 哲學家 monitor 解：state[5]、self[5]，pickup()/putdown()/test()，無死結。

---

# Chapter 7 — Deadlocks

## 7.1 系統模型

行程使用資源序列：**Request → Use → Release**（request/release 常是 system call）。

## 7.2 ★ 死結的四個必要條件（同時成立才死結）
1. **Mutual Exclusion（互斥）**：資源一次一個行程用。
2. **Hold and Wait（持有並等待）**：持有資源又等別的資源。
3. **No Preemption（不可搶占）**：資源只能自願釋放。
4. **Circular Wait（循環等待）**：行程形成等待環。

## 7.3 ★ 資源配置圖（Resource-Allocation Graph）
- **Request edge** Pi→Rj；**Assignment edge** Rj→Pi。
- **無環 → 一定無死結**。
- **有環**：
  - 每種資源**只有一個 instance** → 有環 = **死結**。
  - 每種資源**多個 instance** → 有環**不一定**死結。

## 7.4 處理死結的方法
1. **Prevention（預防）**：破壞四條件之一。
2. **Avoidance（避免）**：用額外資訊（最大需求）動態判斷 → 銀行家演算法。
3. **Detection + Recovery（偵測與復原）**。
4. **Ignore（忽略）**：UNIX/Windows 採用（假裝不會發生，由程式設計師處理）。

## 7.5 死結預防（破壞各條件）
| 破壞條件 | 方法 |
|----------|------|
| Mutual Exclusion | 不可行（如印表機本就需互斥）；唯讀檔案可共享 |
| Hold and Wait | 要求一開始就請求全部資源 / 請求前先釋放所有持有 → **資源利用率低、飢餓** |
| No Preemption | 請求不到就釋放所有持有資源（適合可儲存狀態的資源如記憶體）|
| **Circular Wait** | **資源排序** F:R→N，只能遞增順序請求（見題型 21 證明）|

## 7.6 ★ 死結避免（Deadlock Avoidance）
- **Safe state（安全狀態）**：存在一個**安全序列** <P1,...,Pn>，每個 Pi 的需求可被「現有可用 + 前面 Pj 釋放」滿足。
- **Safe ⊂ 非死結；Deadlock ⊂ Unsafe；但 Unsafe 不一定死結。**

### 資源配置圖演算法（單一 instance）
加入 **claim edge**（虛線 Pi⇢Rj，表未來可能請求）。批准請求只在**不形成環**時。

### ★★ 銀行家演算法（多 instance）
資料結構：**Available**（可用向量）、**Max**（最大需求）、**Allocation**（已配置）、**Need = Max − Allocation**。

**安全演算法：**
1. Work = Available；Finish[i] = false。
2. 找 i 使 Finish[i]==false 且 Needᵢ ≤ Work。找不到 → 跳 4。
3. Work = Work + Allocationᵢ；Finish[i] = true；回 2。
4. 全部 Finish==true → **安全**。

**資源請求演算法：**
1. Requestᵢ ≤ Needᵢ？否 → 錯誤。
2. Requestᵢ ≤ Available？否 → 等待。
3. 假設配置（Available−=Req, Alloc+=Req, Need−=Req），跑安全演算法：安全 → 批准；不安全 → 還原並等待。

> 完整計算範例見 [期末考古題解答.md](期末考古題解答.md) 題型 16、17。

## 7.7 死結偵測
- 單一 instance：**Wait-for graph**（等待圖），有環 = 死結。
- 多 instance：類似銀行家的偵測演算法（用 Request 矩陣）。

## 7.8 死結復原
1. **Process Termination（終止行程）**：全部中止 / 一次中止一個直到無死結。
2. **Resource Preemption（資源搶占）**：選 victim、rollback、避免 starvation。

---

# Chapter 8 — Memory-Management Strategies

## 8.1 背景：保護與位址綁定
- **Base register**（最小合法實體位址）+ **Limit register**（範圍大小）保護記憶體。
- **Address Binding 三時機**：
  - **Compile time** → 絕對碼（位置變要重編譯）。
  - **Load time** → 可重定位碼（載入後不能移動）。
  - **Execution time** → 執行中可移動，需 MMU（通用 OS 採用）。

## 8.2 邏輯 vs 實體位址
- **Logical address**（CPU 產生，= virtual address）↔ **Physical address**（記憶體看到的）。
- **MMU（記憶體管理單元）**：執行時把邏輯→實體（如 relocation register 加總）。
- **Dynamic loading**：routine 被呼叫才載入（省記憶體）。
- **Dynamic linking + 共享函式庫**：連結延到執行時，用 **stub**；多行程共用一份函式庫碼。

## 8.3 連續記憶體配置（Contiguous Allocation）
- **動態配置問題（Dynamic storage-allocation）**：
  - **First-fit**：第一個夠大的洞（**最快**）。
  - **Best-fit**：最小的夠大洞（剩最小碎片，須搜尋整串）。
  - **Worst-fit**：最大的洞（最差）。
- **External fragmentation（外部碎裂）**：總空間夠但不連續。**50% 規則**：N 個區塊會損失 0.5N。
  - 解法：**Compaction（壓縮）**（須執行時動態重定位）。
- **Internal fragmentation（內部碎裂）**：固定分割中配置略大於需求。

## 8.4 ★★ 分頁（Paging）
- **Frame（頁框）**：實體記憶體固定大小塊；**Page（頁）**：邏輯記憶體同大小塊。
- 邏輯位址 = **page number (p) | page offset (d)**。
- 頁大小 2ⁿ，邏輯位址空間 2ᵐ → **高 m−n 位元 = 頁號，低 n 位元 = 偏移**。

### ★ 頁表大小計算公式
```
頁內偏移位元 = log₂(頁大小)
頁號位元 = 邏輯位址位元 − 偏移位元
頁數 = 2^(頁號位元)
頁表大小 = 頁數 × 每筆 entry 大小
```
> 實體記憶體大小只影響 frame number 位元數，**不影響頁表筆數**！

- 分頁**無外部碎裂**，但有**內部碎裂**（最後一頁，平均半頁/行程）。
- **Frame table**：記錄每個 frame 是否空閒、屬於哪個行程的哪一頁。

### Paging 硬體支援
- 頁表放主記憶體，**PTBR（page-table base register）** 指向它 → **兩次記憶體存取**（慢）。
- **TLB（Translation Look-aside Buffer）**：快取（key=頁號, value=frame 號），64~1024 筆。
  - **Hit ratio（命中率）**：在 TLB 找到的比例。
  - **有效存取時間** = hit_ratio×(TLB+mem) + miss_ratio×(TLB+mem+mem)。
  - 例：80% 命中、TLB 20ns、mem 100ns → 0.8×120 + 0.2×220 = **140ns**。
  - **ASID**：區分不同行程的 TLB entry。

### Paging 保護與共享
- **Valid-invalid bit**：標示頁是否在行程合法位址空間。
- **PTLR（page-table length register）**：頁表長度。
- **Shared pages**：reentrant（純）碼可多行程共享（如文字編輯器）。

### 頁表結構
- **Hierarchical（多層）paging**：頁表本身也分頁（two-level、three-level…）。64-bit 不適用（層數太多）。
- **Hashed page table**：虛擬頁號雜湊。
- **Inverted page table**：全系統一張表，每個實體 frame 一筆，含 `<process-id, page-number>`；省記憶體但搜尋慢、難共享。

## 8.5 分段（Segmentation）
- 符合使用者觀點：邏輯位址空間 = 一堆 **segment**（code、global、heap、stack、library…）。
- 邏輯位址 = **<segment-number, offset>**。
- **Segment table**：每筆 = (segment base, segment limit)。

---

# Chapter 9 — Virtual Memory Management

## 9.1 背景
- **虛擬記憶體**：允許行程**部分在記憶體**就能執行。
- 好處：程式不受實體記憶體限制、更多行程並行（提高 CPU 使用率/吞吐量）、I/O 較少程式更快。
- **Sparse address space**：含洞的虛擬位址空間（heap 與 stack 間的空隙）。

## 9.2 ★ 需求分頁（Demand Paging）
- **Lazy swapper（pager）**：頁被需要時才載入；從不用的頁從不載入。
- **Valid-invalid bit**：valid=在記憶體；invalid=非法或在磁碟。
- **Page-fault trap** 處理流程：
  1. 查內部表：合法 or 非法存取。
  2. 非法 → 終止行程。
  3. 合法但未載入 → 找 free frame。
  4. 排程磁碟讀入該頁。
  5. 更新內部表與頁表（valid）。
  6. **重新執行**被中斷的指令。
- **Pure demand paging**：一開始沒有任何頁，需要才 fault 載入。

### ★ 有效存取時間（EAT）
```
EAT = (1 − p) × ma + p × page_fault_time
```
p = page fault 率，ma = 記憶體存取時間。
> 例：ma=200ns、page fault=8ms → EAT = 200 + 7,999,800×p。p 須極小（<0.0000025）才能讓效能降幅<10%。

## 9.3 Copy-on-Write（寫入時複製）
- fork() 後父子**共享同一頁**，標記 copy-on-write；任一方**寫入時才複製**。
- 加速行程建立（Windows XP / Linux / Solaris）。

## 9.4 ★★ 頁面置換（Page Replacement）
- Over-allocate 記憶體 → 可能沒有 free frame → 需置換。
- 置換流程：找 victim frame → （若 dirty 寫回磁碟）→ 讀入新頁 → 更新表 → 重啟行程。
- **Modify bit（dirty bit）**：被改過才需寫回磁碟（省 I/O）。
- **Reference string（參考字串）**：只看頁號的存取序列，用來評估演算法（page fault 數越少越好）。

### 演算法
| 演算法 | 規則 | 重點 |
|--------|------|------|
| **FIFO** | 換最舊的頁 | 簡單；有 **Belady's anomaly**（frame 變多 fault 反而變多）|
| **Optimal (OPT/MIN)** | 換**未來最久不用**的頁 | **最低 fault 率**，但需未來知識（不可實作）|
| **LRU** | 換**最久未使用**的頁 | OPT 的近似，good；用 counter 或 stack 實作；**無 Belady's anomaly**（stack 演算法）|

### LRU 近似
- **Reference bit**：被參考設 1。
- **Additional-reference-bits**：8-bit 歷史，數值最小=LRU。
- **Second-chance（clock）**：FIFO 變體；reference bit=1 給第二次機會（清為 0，移到尾），=0 才換。
- **Enhanced second-chance**：用 (reference, modify) 二位元分四類，換最低非空類別 (0,0) 最優先。

### Counting-based
- **LFU**（最少使用次數換掉）、**MFU**（最多使用次數換掉）— 都不太近似 OPT。

### Page-buffering
- 保留 free frame 池，victim 寫回前先把新頁讀入池中的 frame → 加速。

## 9.5 框架配置（Allocation of Frames）
- **最少框架數**：由指令架構決定（如間接定址需至少 3 frame）。
- **Equal allocation**：m/n 平均分。
- **Proportional allocation**：依行程大小比例分配。
- **Global replacement**：可從別的行程搶 frame（較適應、效能好）。
- **Local replacement**：只從自己的 frame 換（frame 數固定）。

## 9.6 ★ Thrashing（輾轉現象）
- **Thrashing**：行程花在分頁的時間比執行還多（不斷 page fault）。
- 成因：frame 不足 → 一直 fault → CPU 使用率掉 → OS 誤以為要提高多工度 → 更多 fault → 惡性循環。
- 解法：**local replacement**、提供足夠 frame。

### Locality Model（區域性模型）
行程執行時在不同 **locality**（一起使用的頁集合）間移動。frame < locality 大小 → thrashing。

### ★ Working-Set Model（工作集模型）
- **Working-set window Δ**：最近 Δ 次參考。
- **WSSᵢ**：行程 i 工作集大小 ≈ 其 locality。
- 若 **Σ WSSᵢ > 總 frame 數** → 選一行程暫停（swap out），把 frame 給別人。
- 防止 thrashing 同時維持高多工度。

### Page-Fault Frequency（PFF）
直接控制 page fault 率：太高 → 給更多 frame；太低 → 收回 frame。

## 9.7 Memory-Mapped Files（記憶體映射檔案）
- 把磁碟區塊映射到記憶體頁，之後讀寫當成記憶體存取（省 read()/write() 系統呼叫）。
- 寫入不立即寫回磁碟，關檔時才寫回。
- Windows 的共享記憶體（IPC）就是用 memory-mapped file 實作。

---

# ⚠️ 期末易錯重點整理

1. **頁表大小**只看「邏輯位址 + 頁大小」，**與實體記憶體大小無關**。
2. **多層反饋佇列**：I/O 完成回來進 **Q2**（最高層）會搶占低層；被搶占/用完 quantum 才**降層**。
3. **銀行家**：先檢查 Request ≤ Need，再 Request ≤ Available，最後跑安全演算法。
4. **Unsafe ≠ Deadlock**（unsafe 只是可能死結）；**Deadlock ⊂ Unsafe**。
5. **Optimal** 換「未來最久不用」；**LRU** 換「過去最久沒用」。
6. **FIFO** 有 **Belady's anomaly**；**LRU / Optimal**（stack 演算法）**沒有**。
7. **Bounded-buffer**：`wait(empty)` 要在 `wait(mutex)` **之前**，否則死結。
8. **Readers-writers**：第一個 reader `wait(wrt)`、最後一個 reader `signal(wrt)`。
9. **Paging** 無外部碎裂、有內部碎裂；**Segmentation** 有外部碎裂。
10. **死結四條件**缺一不可：Mutual Exclusion / Hold and Wait / No Preemption / Circular Wait。

---

# 關鍵英文術語速查

| 中文 | 英文 |
|------|------|
| 護航效應 | convoy effect |
| 飢餓 / 老化 | starvation / aging |
| 多層反饋佇列 | multilevel feedback queue |
| 時間量子 | time quantum / time slice |
| 處理器親和性 | processor affinity |
| 推移 / 拉移 | push / pull migration |
| 競爭條件 | race condition |
| 臨界區 | critical section |
| 互斥 / 進展 / 有限等待 | mutual exclusion / progress / bounded waiting |
| 號誌 | semaphore |
| 忙等 / 自旋鎖 | busy waiting / spinlock |
| 監視器 / 條件變數 | monitor / condition variable |
| 死結 | deadlock |
| 安全 / 不安全狀態 | safe / unsafe state |
| 安全序列 | safe sequence |
| 銀行家演算法 | banker's algorithm |
| 資源配置圖 | resource-allocation graph |
| 等待圖 | wait-for graph |
| 循環等待 | circular wait |
| 內部 / 外部碎裂 | internal / external fragmentation |
| 壓縮 | compaction |
| 分頁 / 頁框 / 頁 | paging / frame / page |
| 頁表 | page table |
| 轉譯後備緩衝區 | TLB (translation look-aside buffer) |
| 命中率 | hit ratio |
| 反轉頁表 | inverted page table |
| 分段 | segmentation |
| 需求分頁 | demand paging |
| 頁錯誤 | page fault |
| 寫入時複製 | copy-on-write |
| 頁面置換 | page replacement |
| 修改位元 | modify (dirty) bit |
| 參考字串 | reference string |
| 輾轉現象 | thrashing |
| 區域性 / 工作集 | locality / working set |

---

# 歷屆考題出題規律（2011–2016 期末）

| 主題 | 2011 | 2012 | 2013 | 2014 | 2015 | 2016 |
|------|:----:|:----:|:----:|:----:|:----:|:----:|
| 多層反饋佇列甘特圖 | ✓ | ✓ | ✓ | — | — | — |
| 銀行家演算法 | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| 死結概念 / 條件 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 同步程式填空 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 頁表大小計算 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 多層分頁填內容 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 頁面置換計算 | — | — | ✓ | ✓ | — | ✓ |
| Page fault 流程 | — | — | ✓ | — | ✓ | ✓ |
| 虛擬記憶體概念 | — | — | — | — | ✓ | — |

> **結論：分頁（頁表計算+多層分頁）、銀行家、同步程式填空 是三大必拿分題！**
